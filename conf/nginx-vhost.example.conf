server {
    listen  80;
    server_name snailtest.itpp.cn;

    client_max_body_size 12M;

    location / {
        include   uwsgi_params;
        uwsgi_pass   unix:///tmp/uwsgi_mlpm_jobs.sock;
    }
}